# Android 交互级调度延迟分析系统 - 阶段性工作总结 

## 1. 项目背景与目标
本项目旨在针对相机功能，打造一个 **帧对齐、依赖感知（Frame-aligned, dependency-aware）的 Android 调度辅助分析工具**。
传统的性能分析工具往往割裂了“宏观的 App UI 掉帧现象”与“微观的内核任务调度”之间的联系。我们通过结合 **Perfetto Trace Processor** 和 **eBPF (CO-RE)** 技术，在一个闭环流中自动定位 App 侧发生 Jank（卡顿）的具体时间窗口，并精准计算出在该窗口内，UI、RenderThread 以及 Binder 等核心线程到底经受了多长的 **Runnable Delay（就绪等待延迟）**。

## 2. 系统数据流与架构闭环

打通了从 **“系统采集 -> 特征提取 -> 数据聚合映射 -> 结构化分析输出”** 的全栈闭环：

1. **宏观视点：定义 Ground Truth (Perfetto)**
   - 抓取包含 UI 交互卡顿的 Perfetto Trace。
   - 使用 SQL 分析 `actual_frame_timeline_slice` 表，精确定位发生 `App Deadline Missed`（即真实导致画帧超时的严重掉帧）的具体纳秒级时间窗口 (`window_start_ns` 到 `window_end_ns`)。
   - 提取目标应用的静态线程血缘关系（ThreadKey 映射表）。
   - **输出**: `perfetto/output/ebpf_target_windows.json`

2. **微观视点：探测内核调度与依赖边界 (eBPF)**
   - 编译加载基于 libbpf 的 eBPF C 骨架探测程序。
   - 挂载以下 tracepoint：
     - `tracepoint/sched/sched_switch` 与 `tracepoint/sched/sched_wakeup`（调度延迟）
     - `tracepoint/binder/binder_transaction` 与 `tracepoint/binder/binder_transaction_received`（Binder 依赖延迟）
     - `tracepoint/raw_syscalls/sys_enter` (Futex 锁等待/唤醒活动, 按 id==98 过滤)
   - 完全使用 BootTime (`bpf_ktime_get_boot_ns`) 进行时间戳记录，以与 Perfetto 绝对时间对齐。
   - 通过 RingBuffer (4MB, 每1000条 fflush) 推送事件至用户态。
   - **输出**: `ebpf/sched_events.csv` + `ebpf/binder_futex_events.csv`

3. **融合与推演：调度延迟 + 依赖分析聚合器 (Python Analyzer)**
   - 加载 Perfetto 窗口 JSON + sched CSV + binder/futex CSV 三方产物。
   - **Sched 分析**：计算 `Runnable Delay` 与 `Actual Run`。
   - **Binder 依赖分析**：通过 `debug_id` 配对计算端到端 Binder 延迟，构建依赖边。
   - **Futex 活动分析**：按帧统计 `FUTEX_WAIT` / `FUTEX_WAKE` 次数。
   - 增强版角色启发式识别（8 种角色）。
   - **输出**: `delay_analysis_result.json` (threads + binder_edges + futex_activity)

4. **关键路径与根因排序：Critical Path Graph + CriticalScore**
   - 构建每帧的交互关键路径图（Frame node + Thread nodes + 依赖边）。
   - 边类型：`RUNNABLE_WAIT` | `BINDER_CALL` | `FUTEX_WAIT` | `FRAME_DEPENDENCY`。
   - 多维 CriticalScore 评分：frame_overlap + runnable_delay_p95 + binder_centrality + futex_wait + render_path_proximity + repeated_jank − background_penalty。
   - 全局 Top-K 关键线程排名，定位卡顿根因。
   - **输出**: `critical_path_graph.json`
   - `analyze_delays.py` 运行完毕后自动触发此步骤。



```
┌──────────────────────────────────────────────────────────────────┐
│                         Android 设备                             │
│                                                                  │
│  camera_ebpf_android (eBPF)         Perfetto                     │
│  ├─ tracepoint/sched/*               ├─ FrameTimeline            │
│  ├─ tracepoint/binder/*              ├─ 所有 ftrace 事件         │
│  └─ tracepoint/raw_syscalls/*        └─ ...                      │
│       ↓                                    ↓                     │
│  sched_events.csv                  camera_jank.perfetto           │
│  binder_futex_events.csv                                        │
└──────────────────────────────────────────────────────────────────┘
       │                                    │
       │  adb pull                          │  adb pull
       ▼                                    ▼
┌──────────────────┐              ┌──────────────────┐
│  sched_events.csv │              │ camera_jank.perf  │
│  binder_futex.csv │              └────────┬─────────┘
└───┬───────────────┘                       │
    │                                       ▼
    │                            ┌──────────────────────┐
    │                            │  perft  parse_trace.py  │
    │                            │  SQL 查询              │
    │                            │  actual_frame_timeline │
    │                            │  → App Deadline Missed │
    │                            │  → 帧时间窗口          │
    │                            │  → ThreadKey 映射表   │
    │                            └──────────┬───────────┘
    │                                       │
    │                                       ▼
    │                            ┌──────────────────────┐
    │                            │ ebpf_target_windows   │
    │                            │ .json                 │
    │                            │ ┌ jank_frames[]       │
    │                            │ │ ├ frame_token       │
    │                            │ │ ├ window_start_ns   │
    │                            │ │ └ window_end_ns     │
    │                            │ └ threads_map{}       │
    │                            └──────────┬───────────┘
    │                                       │
    ▼                                       ▼
┌──────────────────────────────────────────────────┐
│           ebpf/analyze_delays.py                  │
│                                                   │
│  ① 读入 ebpf_target_windows.json                  │
│  ② 读入 sched_events.csv                         │
│     → 按帧窗口过滤事件 → 逐帧独立处理               │
│     → wakeup←→switch 配对 → Runnable Delay        │
│     → prev←→next 记录 → Actual Run                │
│  ③ 读入 binder_futex_events.csv                  │
│     → debug_id 配对 binder_tx/binder_rx           │
│       → Binder 延迟 + 依赖边                      │
│     → FUTEX_WAIT/WAKE 按帧统计                   │
│  ④ 角色识别 (comm → 8种角色)                     │
│                                                   │
│  ┌── 自动调用 ──┐                                 │
│  │ critical_path.py  │                             │
│  │ ⑤ 构建 per-frame 图                            │
│  │ ⑥ 拓扑DP找关键路径                             │
│  │ ⑦ CriticalScore评分                            │
│  └────────────────┘                                │
└──────────────────────────┬─────────────────────────┘
                           │
           ┌───────────────┼───────────────┐
           ▼               ▼               ▼
┌──────────────────┐ ┌──────────────────┐ ┌──────────────────┐
│ delay_analysis_   │ │ critical_path_   │ │ ═══════════════  │
│ result.json      │ │ graph.json       │ │ 终端打印的报告    │
│                   │ │                  │ │                  │
│ 逐帧的原始数据:   │ │ 图结构数据:       │ │ [Sched] Delays   │
│ └ threads[]       │ │ └ meta           │ │ [Binder] Edges   │
│   └ runnable_delay│ │ └ global_critical│ │ [Futex] Activity │
│   └ actual_run    │ │   _scores[]      │ │ Top-10 Threads   │
│ └ binder_edges[]  │ │ └ frames[]       │ │                  │
│ └ futex_activity[]│ │   └ nodes[]      │ │ ═══════════════  │
│                   │ │   └ edges[]      │ │                  │
│ 给机器看的        │ │   └ critical_path│ │ 给人看的 (滚动)   │
└──────────────────┘ └──────────────────┘ └──────────────────┘
                           │
                           ▼
               ┌──────────────────────┐
               │ generate_report.py    │
               │                      │
               │ 读取两个 JSON          │
               │ 排版 → markdown 表格  │
               │                      │
               │ reports/report_*.md  │
               │ 给人看的 (可保存)      │
               └──────────────────────┘

```

## 3. 已攻克的关键技术难点

在调试并跑通这条链路的过程中，我们解决了一系列非常具有代表性的底层坑：

- **内核进程名 15 字符截断陷阱**
  - **问题**: 用户输入的完整包名（如 `com.google.android.GoogleCamera`），在内核 `task_struct->comm` 字段中会被截断成 `com.google.andr`。导致根据关键字匹配进程永远找不到对应的 UI 进程（反而匹配到了 HAL 层的服务）。
  - **解决**: 调整视角，以果推因。直接在 Perfetto SQLite 表中扫描具备 `App Deadline Missed` 标签的 UPID，反向锁定当前拥有焦点的 Janky 应用，完美绕过字符串截断。
- **PID 复用引发的“幽灵线程”乱入**
  - **问题**: 仅凭 TID 匹配，容易在时间流中遇到 PID 被回收后分配给别的进程的情况，导致分析错乱。
  - **解决**: 引入以 UID 与 TGID 为中心的“Identity Layer Filtering”，利用 Perfetto 的静态血缘作为字典，剔除非目标 UID 的杂音线程。
- **高频事件下的 eBPF I/O 阻塞丢失数据**
  - **问题**: `sched_events.csv` 只有头一两毫秒的事件，由于 `while(1)` 读取轮询或用户态退出的逻辑缺陷，导致大规模调度事件因底层缓冲未 `fflush` 或者提早 `fclose` 而丢失。
  - **解决**: 修正了 C 用户态代码的退出生命周期设计，通过实时 `fflush()` 将 RingBuffer 的高频数据落盘，保证 trace 不会由于瞬时突发而断片。
- **异构系统的时钟域对齐**
  - 使用 `bpf_ktime_get_boot_ns()` 成功保证了 eBPF 输出的时间戳可以和 Perfetto 中采用的 BootTime 基准相对齐，使微观事件能够无缝嵌入宏观掉帧窗口中。

## 4. 当前标准操作工作流 (SOP)

1. **开始 eBPF 记录**: 在 Android 终端启动 `./camera_ebpf`，同时采集 sched + binder + futex 事件。
2. **收集卡顿 Trace**: 开启 Perfetto，用户在 App 上执行产生卡顿的操作。
3. **结束记录**: 停止 Perfetto 形成 `.perfetto` 文件；`Ctrl+C` 中止 eBPF，生成 `sched_events.csv` + `binder_futex_events.csv`。
4. **生成卡顿窗口**: `python3 perfetto/parse_trace.py trace.perfetto <app_name>` → `ebpf_target_windows.json`
5. **多维分析**: `python3 ebpf/analyze_delays.py --json ... --csv sched_events.csv --binder binder_futex_events.csv`
6. **读取成果**: `delay_analysis_result.json` 包含每帧的调度延迟、Binder 依赖边、Futex 活动。

## 5. 更新日志 

### 5.1 2026-06-02: Critical Path Graph + CriticalScore (含图分析)
- ✅ 新建 `ebpf/critical_path.py`
- ✅ **图结构**: Frame node + Thread node, 边类型 `RUNNABLE_WAIT` | `BINDER_CALL` | `FUTEX_WAIT`
- ✅ **关键路径分析** (`find_critical_paths`): 拓扑 DP 找每帧 DAG 最长阻塞链, 回溯标出 on-critical-path 节点
- ✅ **根因 vs 受害者区分**: 通过 `block_ratio` 判断节点是上游阻塞源还是下游被牵连
- ✅ **CriticalScore 公式 v2** (新增 h 维度):
  ```
  score = a·overlap + b·p95 + c·binder + d·futex
        + e·rpp + f·repeated + h·on_critical_path - g·bg
  ```
  其中 h=1.8 (图分析权重), on_critical_path = 线程出现在关键路径上的帧占比
- ✅ 输出中标记 `★ROOT` 根因节点 (on_cp>0.3 且 block_ratio>0.5)
- ✅ 全流程联动: `analyze_delays.py` → `critical_path.py` (build → find_critical_paths → compute_scores)

### 5.2 2026-06-01: eBPF 采集面扩展 (Binder + Futex)
- ✅ 新增 `tp/binder/binder_transaction` + `binder_transaction_received` 跟踪点
- ✅ 新增 `tp/syscalls/sys_enter_futex` 跟踪点
- ✅ 双 CSV 分流输出 (sched + binder/futex)，RingBuffer 扩容至 512KB
- ✅ Python 分析器支持三维报告 + 增强角色识别

### 5.3 2026-06-03: 观察者效应优化 + 数据准确性修复

#### 内核态 UID 预过滤 (futex)
- ✅ BPF 侧新增 `target_uid` + `skip_by_uid()`，内核侧按 UID 过滤 futex 事件
- ✅ 仅 filt futex（raw_syscalls 最高频），sched/wakeup/binder 保持全量以保留跨进程依赖
- ✅ 用户态 C 代码支持 `-u <uid>` 参数
- ✅ `auto_run.py` 启动前自动获取 UID 并传入 eBPF

#### 跨帧延迟污染修复
- ✅ 修复: `pending_wakeups` 按帧独立，杜绝前一帧 wakeup 穿越到后一帧
- ✅ 增加物理约束 `rd <= frame_dur`，拒绝超过帧窗口的无效延迟

### 5.4 2026-06-03: 数据质量与可读性修复 (P95 + KernelWorker + Binder展示 + 关键路径)

#### P95 使用累加值而非个体样本
- ✅ `analyze_delays.py`: 新增 `frame_delay_events[frame][tid]` 记录每个 wakeup→switch 延迟样本
- ✅ `critical_path.py`: P95 从 `gt["delay_events"]`（数百个个体样本）计算，而非 `delay_samples`（7 个帧累加值）
- ✅ `generate_report.py`: 每帧线程表新增 "P95 Delay" 列

#### swapper/kworker 污染 Top-K
- ✅ `identify_role()` (两个文件): 新增 `KernelWorker` 角色检测 (`swapper/*`, `kworker/*`, `kswapd0`)
- ✅ `critical_path.py`: `build_graphs()` 标记 `gt["is_kernel"]=True`；`compute_critical_scores()` 跳过内核线程
- ✅ 效果: Top-15 零内核线程，评分线程从 345→268

#### Binder to_thread=0 显示优化
- ✅ `analyze_delays.py`: `to_thread=0` 显示为 `any`（Android Binder 通常不指定目标线程，此为正常行为）
- ℹ️ DAG 构建不受影响：Binder 边匹配靠 `debug_id` 配对 tx/rx

#### 关键路径 TOP-3 + Binder 边纳入
- ✅ `critical_path.py` `find_critical_paths()`: 从单路径重写为 TOP-3，遍历所有 Thread→Frame 边回溯 BINDER_CALL 链
- ✅ `generate_report.py`: 渲染 `critical_paths` 列表，展示边类型摘要 (BINDER_CALL → RUNNABLE_WAIT)
- ✅ 多跳路径示例: `Frame → lwis_I2C_Bus_1 → binder:955_2`

### 5.5 2026-06-03: Ring Buffer 溢出修复 + fflush 优化
- ✅ Ring Buffer: 512KB → **4MB** (缓冲时间 0.12s→0.95s @92K事件/s)
- ✅ fflush: 每事件一次 → **每 1000 条一次** (消除磁盘 I/O 阻塞 poll 线程)
- ✅ Shutdown 时 final fflush + 打印事件总数
- ✅ 效果: 36s trace 采集 3.3M sched + 224K binder 事件, binder 配对率 99.5%

### 5.6 2026-06-03: root_cause.py — 延迟归因分析
- ✅ 新建 `ebpf/root_cause.py`
- ✅ 对 Top-K 线程逐帧汇总: 调度竞争(Runnable Delay) + Binder IPC 耗时 + Futex WAIT 次数
- ✅ 三个维度以绝对值列出, 不做假百分比
- ✅ 核心发现: Top-K 线程延迟几乎全是调度竞争(ms级), Binder 贡献极小(µs级)

### 5.7 2026-06-03: Tuning Profile Generator (safe_hint_engine.py 重构)
- ✅ 从"实时 Safe Hint Engine (TTL 300s)"重构为"离线 Tuning Profile Generator (持久化)"
- ✅ 输出 `tuning_profile.json` + `apply_tuning.sh` (可直接 adb shell 执行)
- ✅ 脚本通过 `/proc/PID/task/*/comm` 按线程名查找动态 TID
- ✅ 三种部署: 临时(adb) / Magisk(`/data/adb/service.d/`) / init.rc
- ✅ uclamp_boost / cpu_affinity_big / cgroup_fg / monitor 四种动作类型

## 6。待解决

### 数据采集
- 全量采集 36s→3.3M sched事件+224K binder事件(数百MB)，需内核态 frame-window 按需过滤
- Binder 链在关键路径中频率较低：跨进程 rx_thread 调度数据常不在帧窗口内

### 分析深度
- Futex 只有 WAIT/WAKE 次数，无法计算每次阻塞的时长
- 缺少 CPU frequency / IRQ / thermal 探针，无法区分"调度竞争"还是"低频运行"

### 调优闭环
- Tuning Profile 基于单次 trace，缺多次采样的A/B验证框架
- 无设备端在线分析能力（需 PC 离线处理）